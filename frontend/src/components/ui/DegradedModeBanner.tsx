import React from 'react';
import { PageBanner } from './PageBanner';

export function DegradedModeBanner() {
  return (
    <PageBanner 
      title="Limited Evidence"
      description="Live research was only partially completed. The final answer may have reduced evidence coverage."
    />
  );
}
